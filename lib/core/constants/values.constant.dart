/// Constants class that holds pagination-related values used across the application
class Values {
  /// Default number of items to be displayed per page
  static const int pageSize = 10;

  /// Starting page index for pagination, typically zero-based
  static const int pageIndex = 0;
}
